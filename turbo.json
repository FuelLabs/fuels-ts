{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "setup": {},
    "prebuild": {
      "dependsOn": ["^prebuild", "setup"]
    },
    "build": {
      "dependsOn": ["^build", "prebuild"],
      "outputs": ["dist/**"]
    }
  }
}
