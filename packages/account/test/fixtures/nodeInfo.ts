import type { NodeInfoJson } from '../../src';

export const MOCK_NODE_INFO: NodeInfoJson = {
  utxoValidation: true,
  vmBacktrace: true,
  maxTx: '4064',
  maxDepth: '10',
  nodeVersion: '0.22.0',
};
