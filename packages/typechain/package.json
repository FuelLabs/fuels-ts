{
  "name": "@fuels-ts/typechain",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "test": "mocha -r ts-node/register 'src/**/**.test.ts'",
    "test:coverage": "nyc --reporter=text mocha -r ts-node/register 'src/**/**.test.ts'",
    "example": "npm run build && typechain --target=. --out-dir=example/types 'example/**.json'"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "typechain": "^5.2.0"
  },
  "devDependencies": {
    "chai": "^4.3.4"
  }
}
