{
  "private": true,
  "name": "fuel-gauge",
  "version": "0.0.1",
  "description": "",
  "author": "Fuel Labs <contact@fuel.sh> (https://fuel.network/)",
  "scripts": {
    "pretest": "pnpm build:forc",
    "build:forc": "pnpm fuels build --deploy",
    "type:check": "tsc --noEmit",
    "test": "cd ../.. && pnpm run test:filter packages/fuel-gauge",
    "test:update-fixtures": "UPDATE_FIXTURES=true pnpm run test"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "fuels": "workspace:*"
  },
  "devDependencies": {
    "@fuel-ts/abi": "workspace:*",
    "@fuel-ts/account": "workspace:*",
    "@fuel-ts/errors": "workspace:*",
    "@fuel-ts/merkle": "workspace:*",
    "@fuel-ts/utils": "workspace:*"
  }
}
