{
  "name": "@fuels-ts/contract",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "test": "mocha -r ts-node/register 'src/**/**.test.ts'",
    "test:coverage": "nyc --reporter=text mocha -r ts-node/register 'src/**/**.test.ts'"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@fuels-ts/abi-coder": "../abi-coder"
  },
  "devDependencies": {
    "chai": "^4.3.4"
  }
}
