{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "prebuild"],
      "outputs": ["dist/**"],
      "inputs": ["src/**", "!src/api", ".vitepress/**", "types/**"]
    }
  }
}
